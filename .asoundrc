pcm.!default {
    type pulse
    fallback "sysdefault"
    hint {
        description "Default Audio Device"
    }
}

ctl.!default {
    type pulse
    fallback "sysdefault"
}

# Virtual MIDI device
pcm.virtual {
    type hw
    card 0
}

ctl.virtual {
    type hw
    card 0
}
